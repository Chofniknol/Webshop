export const environment = {
  production: true,
  BASIC_URL : "https://iprwc-backend-api.onrender.com/" //added this here because error Property 'BASIC_URL' does not exist on type '{ production: boolean; }'
};

// need this for testing: BASIC_URL : "http://localhost:8080/"
